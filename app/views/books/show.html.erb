<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('model.show.edit'), edit_book_path(@book) %> |
  <%= link_to t('model.show.back_to_list', name: Book.model_name.human), books_path %>

  <%= button_to t('model.show.destroy'), @book, method: :delete %>
</nav>
